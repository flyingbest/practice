#include <stdio.h>
#include <stdlib.h>

/*
int main(int argc, char* argv[]){
	int valid = false;
	char str1[8];
	char str2[8];

	//next_tag(str1);
	str1 = {'S', 'T', 'A', 'R', 'T'};
	gets(str2);
	if( strncmp(str1, str2, 8) == 0 ){
		valid = true;
	printf("buffer1 : str1(%s), str2(%s), valid(%d)\n", str1, str2, valid);
	
	return 0;
}
*/

int main(){
	bool valid = false;
	
	printf("%d\n", valid);
	return 0;
}
